apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: {{.prometheus_url}}
    isDefault: true

  - name: Loki
    type: loki
    access: proxy
    url: {{.loki_url}}
